"use client";

// Provider profile settings reuses the same form logic as customer settings.
// We re-export the same UI under the provider dashboard layout.
export { default } from "@/app/dashboard/customer/settings/page";
